name: Publish manylinux_ghr_x86_64 image
on:
  workflow_dispatch:
  push:
    branches: ['main']
    paths:
    - dockerfiles/manylinux_ghr_x86_64.Dockerfile
    - .github/workflows/publish_manylinux_ghr_x86_64.yml

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-image:
    uses: ./.github/workflows/publish_dockerfile.yml
    with:
        DOCKERFILE_NAME: manylinux_ghr_x86_64
        PLATFORMS: linux/amd64
